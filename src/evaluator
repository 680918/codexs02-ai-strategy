from metrics import sharpe, max_drawdown

def evaluate(equity_curve):

    returns = [
        equity_curve[i] - equity_curve[i-1]
        for i in range(1, len(equity_curve))
    ]

    score = sharpe(returns) - max_drawdown(equity_curve)

    return score